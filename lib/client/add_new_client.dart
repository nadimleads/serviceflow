import 'package:flutter/material.dart';

class AddNewClient extends StatelessWidget {
  const AddNewClient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Add Client')));
  }
}
